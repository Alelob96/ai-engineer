# Repository guidance

Edit the installable skill under `skills/ai-engineer/`. Keep `SKILL.md` concise, with direct links to task-specific references. Verify version-sensitive OpenAI and LangGraph claims against official documentation and avoid hard-coded prices. Do not add executable examples unless they are run and tested. Run `python scripts/validate_skill.py` after changes. Explicit user instructions override skill guidance.
